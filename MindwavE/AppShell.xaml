<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MindwavE.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MindwavE"
    Title="MindwavE">

    <FlyoutItem x:Name="MainFlyoutItem" Title="Main">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem x:Name="HomeFlyoutItem" Title="Home" IsEnabled="False">
        <ShellContent
            ContentTemplate="{DataTemplate local:HomePage}"
            Route="HomePage" />
    </FlyoutItem>

    <FlyoutItem x:Name="TrackingFlyoutItem" Title="Tracking" IsEnabled="False">
        <ShellContent
            ContentTemplate="{DataTemplate local:TrackingPage}"
            Route="TrackingPage" />
    </FlyoutItem>

    <FlyoutItem x:Name="ChatFlyoutItem" Title="Chat (AI Bot)" IsEnabled="False">
        <ShellContent
            ContentTemplate="{DataTemplate local:ChatPage}"
            Route="ChatPage" />
    </FlyoutItem>

</Shell>
